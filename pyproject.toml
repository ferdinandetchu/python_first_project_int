[project]
name = "python-one"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ferdinandetchu", email = "ferdinandetchu@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic-settings>=2.15.0",
]

[project.scripts]
python-one = "python_one:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
